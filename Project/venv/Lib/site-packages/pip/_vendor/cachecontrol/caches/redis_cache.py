# SPDX-FileCopyrightText: 2015 Eric Larson
#
# SPDX-License-Identifier: Apache-2.0
<<<<<<< HEAD:Project/venv/Lib/site-packages/pip/_vendor/cachecontrol/caches/redis_cache.py

from __future__ import division
=======
from __future__ import annotations


from datetime import datetime, timezone
from typing import TYPE_CHECKING
>>>>>>> d82773e6416954b2d4f26b7af1800a1f26f6a02e:Project/venv/lib/python3.8/site-packages/pip/_vendor/cachecontrol/caches/redis_cache.py

from pip._vendor.cachecontrol.cache import BaseCache

if TYPE_CHECKING:
    from redis import Redis


class RedisCache(BaseCache):
    def __init__(self, conn: Redis[bytes]) -> None:
        self.conn = conn

    def get(self, key: str) -> bytes | None:
        return self.conn.get(key)

    def set(
        self, key: str, value: bytes, expires: int | datetime | None = None
    ) -> None:
        if not expires:
            self.conn.set(key, value)
        elif isinstance(expires, datetime):
<<<<<<< HEAD:Project/venv/Lib/site-packages/pip/_vendor/cachecontrol/caches/redis_cache.py
            expires = expires - datetime.utcnow()
            self.conn.setex(key, int(expires.total_seconds()), value)
=======
            now_utc = datetime.now(timezone.utc)
            if expires.tzinfo is None:
                now_utc = now_utc.replace(tzinfo=None)
            delta = expires - now_utc
            self.conn.setex(key, int(delta.total_seconds()), value)
>>>>>>> d82773e6416954b2d4f26b7af1800a1f26f6a02e:Project/venv/lib/python3.8/site-packages/pip/_vendor/cachecontrol/caches/redis_cache.py
        else:
            self.conn.setex(key, expires, value)

    def delete(self, key: str) -> None:
        self.conn.delete(key)

    def clear(self) -> None:
        """Helper for clearing all the keys in a database. Use with
        caution!"""
        for key in self.conn.keys():
            self.conn.delete(key)

    def close(self) -> None:
        """Redis uses connection pooling, no need to close the connection."""
        pass
